#debug_level 2
###
 #set WORK_RAM_ADDR 0x00140000
 set WORK_RAM_ADDR 0x0
# set WORK_RAM_SIZE 0x800
 set WORK_RAM_SIZE 0x0

adapter driver remote_bitbang
remote_bitbang host localhost

remote_bitbang port 9999

set _CHIPNAME riscv
jtag newtap $_CHIPNAME cpu -irlen 5 -expected-id 0x249511C3

foreach t [jtag names] {
    puts [format "TAP: %s\n" $t]
}

set _TARGETNAME $_CHIPNAME.cpu
# -------------------------------------------------------------------
# IMPORTANT:
#   core with no RAM must run without a work-area.
#   Explicitly disable work-area by setting:
#     -work-area-phys 0
#     -work-area-size 0
# -------------------------------------------------------------------
target create $_TARGETNAME riscv \
    -chain-position $_TARGETNAME \
    -work-area-phys  $WORK_RAM_ADDR \
    -work-area-size  $WORK_RAM_SIZE \
    -rtos none

riscv set_reset_timeout_sec 500
riscv set_command_timeout_sec 500

puts "scan_chain:\n" 
# dump jtag chain
scan_chain
puts "----\n" 
init

halt
echo "Ready for Remote Connections"
